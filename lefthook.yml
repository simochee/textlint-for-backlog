pre-commit:
  commands:
    biome:
      glob: "*"
      stage_fixed: true
      run: |-
        npx prettier --write --ignore-unknown {staged_files}

    sort-package-json:
      glob: package.json
      stage_fixed: true
      run: |-
        npx sort-package-json
