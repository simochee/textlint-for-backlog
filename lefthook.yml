pre-commit:
  commands:
    biome:
      glob: "*"
      stage_fixed: true
      run: |-
        npx prettier \
          --write \
          --ignore-unknown \
          --plugin=prettier-plugin-packagejson \
          {staged_files}
